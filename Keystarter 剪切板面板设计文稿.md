# Keystarter 剪切板面板设计文稿

## 一、定位

复刻 Clipy 的核心体验，无主界面，无 Dock 图标。`Cmd+Shift+V` 唤起，鼠标位置弹出，选中即粘贴回原 App 光标处。数据存 SQLite，与 Keystarter 主启动器共用存储层。

---

## 二、交互模型

### 唤起
- 全局快捷键 `Cmd+Shift+V`
- 面板在**鼠标当前位置**弹出，向上展开（鼠标 Y 坐标减面板高度）
- 屏幕边缘自动偏移：靠近右边向左移，靠近底部向上弹
- `nonactivatingPanel`：面板显示时不抢走当前 App 焦点，保证粘贴目标不丢

### 分组结构
- 每组固定 **10 条**记录
- 用户可配置**显示多少组**，默认 3 组（即最多 30 条）
- 每组是一个**子菜单**，组内条目按时间倒序排列
- 组内条目**前面带编号**（1–10），编号用于快速选择
- 组与组之间用分隔线或组标题区分

### 选择与粘贴
- 键盘上下键在组内移动，左右键或 Tab 切换组
- 数字键 `1`–`9`、`0` 直接选中对应编号条目
- 回车 / 双击：写回剪切板 → 关闭面板 → 模拟 `Cmd+V` 粘贴到原 App
- `Esc`：关闭面板，不粘贴

### 粘贴流程
1. 记录当前前台 App（唤起面板前）
2. 用户选中条目，面板关闭
3. 内容写回 `NSPasteboard`
4. 激活之前的前台 App
5. 模拟 `Cmd+V`（需 Accessibility 权限）

---

## 三、视觉设计

### 面板
- `NSPanel`，无边框，圆角 8px，半透明毛玻璃背景
- 宽度约 320px，高度根据组数动态计算
- 阴影，置顶，可加入所有 Space

### 列表行
- 行高 40–48px
- 左侧：编号 + 缩略图/图标
- 右侧：内容摘要
  - 文本：截断显示前几十个字符
  - 图片：等比缩放缩略图（约 32×32 或 40×40）
- 选中行高亮

### 分组标题
- 每组顶部一个细标题栏，显示“第 1 组”或时间范围
- 或者用细分隔线代替标题，视觉更轻

---

## 四、存储设计

### SQLite 表
```sql
CREATE TABLE clipboard (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    type TEXT NOT NULL,          -- "text" / "image"
    content TEXT NOT NULL,       -- 文本内容，或图片文件路径
    hash TEXT NOT NULL,          -- 内容哈希，用于去重
    created_at INTEGER NOT NULL  -- Unix 时间戳
);
```

### 图片存储
- 图片文件存 `~/Library/Application Support/Keystarter/clipboard/`
- 文件名用 UUID，SQLite 只存路径
- 避免 BLOB 塞进数据库导致体积膨胀

### 去重
- 插入前比对最近一条记录的 hash
- 相同则跳过，不产生重复条目

### 容量控制
- 条数上限：默认 500 条
- 时间上限：默认 30 天
- 超过上限自动清理最旧记录，图片文件同步删除

---

## 五、监听机制

- `NSPasteboard.general.changeCount` 是唯一可靠信号
- 定时器轮询，间隔 **0.3 秒**
- `changeCount` 变化时读取内容
- 优先读文本，其次读图片
- 检测 `org.nspasteboard.ConcealedType` 标记，**跳过密码管理器等敏感内容**

---

## 六、权限

- 模拟 `Cmd+V` 需要 **Accessibility 权限**
- 首次调用 `CGEvent.post` 时系统弹窗，用户需去系统设置授权
- 启动时检测权限，未授权则引导用户开启

---

## 七、与主启动器的关系

| 入口 | 快捷键 | UI |
|------|--------|-----|
| 主启动器 | `Cmd+Space` | 输入框 + 结果列表 |
| 剪切板面板 | `Cmd+Shift+V` | 分组列表 + 编号 |

- 两者共用 SQLite 和 `AppDelegate`
- 两个 `HotkeyManager` 实例分别注册
- 剪切板面板不需要输入框和模糊搜索，实现比主窗口简单

---

## 八、配置项

- 快捷键 (可重新录制)
- 显示组数（默认 30）
- 每组条数（默认 10，固定或可调）
- 容量上限（条数 / 天数）
- 是否记录图片
- 是否启用编号快速选择

---

## 九、待确认的取舍

1. **分组 vs 连续滚动**：分组更符合你说的“子菜单”结构，但连续滚动操作更顺。可以先做分组，用一段时间再决定。
2. **是否加历史搜索**：加了搜索框，分组结构会变别扭。建议先不做，等真有需求再加。
3. **编号键位**：`1`–`9`、`0` 对应 10 条。

---
